<Page
    x:Class="LabsTogether2.src.pages.Lab12"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LabsTogether2.src.pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="btnSave" Label="Сохранить" Icon="Save" Click="BtnSave_Click_Async" />
            <AppBarButton x:Name="btnOpen" Label="Открыть" Icon="OpenFile" Click="BtnOpen_Click_Async" />
        </CommandBar>
    </Page.BottomAppBar>
    
    
    <Grid Background="White">
        <TextBox x:Name="tbWindow" AcceptsReturn="True" VerticalAlignment="Stretch" TextWrapping="Wrap" ScrollViewer.IsVerticalRailEnabled="True"/>
    </Grid>
</Page>
